{% extends 'layout.njk' %}

{% block content %}
  <div class="govuk-body">
    <h1>Calculation Result</h1>
    {% if model.entitled %}
      We calculate that you would be entitled to a payment of £{{model.payment}} for carrying out that action.
    {% else %}
      You are not entitled to carry out that Action on that Parcel.
    {% endif %}
  </div>
{% endblock %}